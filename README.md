# Oryon
Oryon is a modern programming language featuring Dual-Mode Execution, allowing code to run through either an AST-walking interpreter or a native compiler—all within a single unified toolchain.
